server:
    interface: 0.0.0.0@5300
    interface: 0.0.0.0@53
    access-control: 127.0.0.0/8 allow
    access-control: 192.168.X.X/24 allow
    access-control: 172.16.0.0/12 allow
    access-control: 10.0.0.0/8 allow
    access-control: 172.17.0.0/16 allow
    access-control: 172.18.0.0/16 allow
    access-control: 0.0.0.0/0 refuse

    verbosity: 1

    # Desabilita chroot para permitir acesso ao socket dnstap em /var/dnstap
    chroot: ""

    module-config: "subnetcache validator iterator"

    max-client-subnet-ipv4: 24
    max-client-subnet-ipv6: 48
    min-client-subnet-ipv4: 24
    min-client-subnet-ipv6: 48

    client-subnet-zone: "."

    msg-cache-size: 64m
    rrset-cache-size: 128m

    num-threads: 8
    so-rcvbuf: 4m
    so-sndbuf: 4m
    outgoing-range: 4096

    infra-cache-numhosts: 10000
    infra-cache-slabs: 8
    msg-cache-slabs: 8
    rrset-cache-slabs: 8
    key-cache-slabs: 8

    prefetch: yes
    prefetch-key: yes
    cache-min-ttl: 300
    cache-max-ttl: 86400

    serve-expired: yes
    serve-expired-ttl: 86400

    target-fetch-policy: "3 2 1 0 0"
    harden-below-nxdomain: yes
    harden-referral-path: no
    harden-dnssec-stripped: yes
    qname-minimisation: yes
    aggressive-nsec: yes
    rrset-roundrobin: no

    hide-identity: yes
    hide-version: yes

    ip-ratelimit: 500
    ip-ratelimit-slabs: 8

    edns-tcp-keepalive: yes
    edns-buffer-size: 1232
    max-udp-size: 1232

    tcp-idle-timeout: 120

    auto-trust-anchor-file: "/opt/unbound/etc/unbound/var/root.key"

domain-insecure: "flexnet.in"

forward-zone:
    name: "flexnet.in"
    forward-addr: SEU_DNS_INTERNO

dnstap:
    # Habilita o dnstap (obrigatorio)
    dnstap-enable: yes

    # Modo bidirecional (handshake fstrm com o coletor)
    dnstap-bidirectional: yes

    # Socket Unix compartilhado com o coletor Go
    dnstap-socket-path: "/var/dnstap/dnstap.sock"

    # Captura respostas e queries dos clientes
    dnstap-log-client-response-messages: yes
    dnstap-log-client-query-messages: yes

    # Nao captura trafego interno (reduz volume)
    dnstap-log-resolver-query-messages: no
    dnstap-log-resolver-response-messages: no
    dnstap-log-forwarder-query-messages: no
    dnstap-log-forwarder-response-messages: no
